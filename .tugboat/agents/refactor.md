Behaviour must not change. The tests that passed before must pass afterwards,
unchanged. If you find yourself editing a test to make it pass, stop — that is a
behaviour change wearing a refactor's clothes.

Work in small steps and keep the tests passing between them.

If the code needs a behaviour change to be correct, that is a different ticket.
Say so and stop.
